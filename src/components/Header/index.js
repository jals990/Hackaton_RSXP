import React from "react";
import "./style.css";

const Header = () => (
  <section className="instructions">
    <header>
      <h1>Hackaton</h1>
    </header>
  </section>
);

export default Header;
